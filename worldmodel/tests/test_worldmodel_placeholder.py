"""Placeholder so pytest has something to collect before worldmodel has real code.

Replace with real tests as soon as worldmodel gets its first implementation
(see CLAUDE.md section 6, build sequence).
"""


def test_worldmodel_placeholder() -> None:
    assert True
