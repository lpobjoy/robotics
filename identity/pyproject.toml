[project]
name = "identity"
version = "0.1.0"
requires-python = ">=3.11"
description = "Machine identity for adapters (OIDC client-credentials or mTLS) authenticating to the router and EAM. See CLAUDE.md section 4.7. Not yet built (step 5)."
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/identity"]
