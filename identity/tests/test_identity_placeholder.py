"""Placeholder so pytest has something to collect before identity has real code.

Replace with real tests as soon as identity gets its first implementation
(see CLAUDE.md section 6, build sequence).
"""


def test_identity_placeholder() -> None:
    assert True
