"""Placeholder so pytest has something to collect before telemetry has real code.

Replace with real tests as soon as telemetry gets its first implementation
(see CLAUDE.md section 6, build sequence).
"""


def test_telemetry_placeholder() -> None:
    assert True
