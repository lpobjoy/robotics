"""Placeholder so pytest has something to collect before router has real code.

Replace with real tests as soon as router gets its first implementation
(see CLAUDE.md section 6, build sequence).
"""


def test_router_placeholder() -> None:
    assert True
