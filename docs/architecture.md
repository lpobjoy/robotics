# Architecture

Not yet written. Due at build-sequence step 14 (CLAUDE.md section 6): the
four layers (world model, agent/router, fleet abstraction, platform) with a
Mermaid diagram, once those layers exist to describe accurately.
