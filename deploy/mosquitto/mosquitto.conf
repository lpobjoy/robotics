# Local-dev broker only. Anonymous access is fine here because this never
# leaves the laptop; do not carry this config anywhere real.
listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_dest stdout
