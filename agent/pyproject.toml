[project]
name = "agent"
version = "0.1.0"
requires-python = ">=3.11"
description = "Pydantic AI mission agent: reads released work orders, plans missions, dispatches via the router, supervises execution, and escalates to a human on uncertainty. See CLAUDE.md section 4.3. Not yet built (step 6)."
dependencies = [
    "pydantic>=2",
    "pydantic-ai>=0.0.30",
    "mcp>=1.0",
    # Not just a test/dev tool: mission_agent.py's mcp_toolset() spawns
    # `python -m mcp_server` as a real subprocess at runtime, using the
    # agent's own interpreter (sys.executable) -- mcp_server has to be
    # installed in this same environment for that to exist to spawn.
    "mcp_server",
]

[dependency-groups]
dev = [
    "eam",
    "worldmodel",
    "router",
    "audit",
    "identity",
    "uvicorn>=0.30",
    "httpx>=0.27",
    "anyio",
]

[tool.uv.sources]
eam = { workspace = true }
worldmodel = { workspace = true }
router = { workspace = true }
audit = { workspace = true }
identity = { workspace = true }
mcp_server = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent"]
