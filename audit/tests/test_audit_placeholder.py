"""Placeholder so pytest has something to collect before audit has real code.

Replace with real tests as soon as audit gets its first implementation
(see CLAUDE.md section 6, build sequence).
"""


def test_audit_placeholder() -> None:
    assert True
