"""Placeholder so pytest has something to collect before spot has real code.

Replace with real tests as soon as spot gets its first implementation
(see CLAUDE.md section 6, build sequence).
"""


def test_spot_placeholder() -> None:
    assert True
