"""Placeholder so pytest has something to collect before mcp_server has real code.

Replace with real tests as soon as mcp_server gets its first implementation
(see CLAUDE.md section 6, build sequence).
"""


def test_mcp_server_placeholder() -> None:
    assert True
